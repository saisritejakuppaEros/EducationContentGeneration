# Legacy pipeline scripts

These scripts belong to an **older** chapter → video flow (problem statement, image/video beats, separate Flux/LTX prompt steps).

The current pipeline uses `stages/` and is orchestrated by `../run_pipeline.py`. Mapping to the new stages is documented in [../../plan.md](../../plan.md).

Run from repo root, e.g.:

```bash
python scripts/legacy/generate_ps_math_linkup.py
```
